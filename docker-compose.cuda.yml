services:
  decidealot:
    build:
      context: .
      dockerfile: Dockerfile.cuda
    image: psyb0t/decidealot:local-cuda
    gpus: all
    tmpfs:
      # Triton loads CUDA helpers compiled at runtime from this cache.
      - /var/cache:rw,exec,nosuid,nodev,size=512m,uid=${DECIDEALOT_UID:-1000},gid=${DECIDEALOT_GID:-1000},mode=0755
    environment:
      DECIDEALOT_DEVICE: cuda
      DECIDEALOT_IMAGE_VARIANT: cuda
